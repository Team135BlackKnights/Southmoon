# Core python deps
coremltools==8.1
numpy==2.2.1
opencv-contrib-python==4.10.0.84
opencv-python==4.10.0.84
pillow==11.1.0
pandas==2.3.3

# NetworkTables / robotpy
pyntcore==2025.1.1.0
robotpy-wpimath==2025.1.1.0

# macOS AVFoundation bridge
pyobjc-framework-AVFoundation==10.3.2

# Basler pypylon (only required for Basler cameras)
# Note: pypylon may require Basler Pylon SDK installed on the system.
pypylon==4.1.0

# USB and libusb backend
# Pin format fixed (no extra spaces). libusb1 provides a Python binding helper.
pyusb==1.2.1
libusb1==3.3.1

# Optional: add other extras here
ultralytics